php-ip-logger
=============

A simple php mysql ip logging page.


IP log/display page writen in PHP and MySQL.

The MySQL commands are in the comments for the page.

Ideas:
* Expand logging options
* Only display if passed key in $_GET
* Auth
* Whitelist and blacklist drop down
* JSON-able

